# Bundled Model Assets

This directory is populated locally before building a full offline APK.

Run from the repository root:

```powershell
.\scripts\sync-models.ps1
```

The copied model files are ignored by Git.
